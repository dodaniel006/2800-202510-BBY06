<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= title %></title>
    <link rel="icon" type="image/png" href="/images/logo.png" />
    <link rel="stylesheet" href="/css/base.css" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"
    />

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <% if (mapPage) { %>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/ol@v10.1.0/ol.css"
    />
    <script src="https://cdn.jsdelivr.net/npm/ol@v10.1.0/dist/ol.js"></script>
    <% } %> <% if (pageCSS) { %>
    <link rel="stylesheet" href="<%= pageCSS %>" />
    <% } %>
  </head>
  <body>
    <div class="page-wrapper">
      <% const navVisible = (typeof showNav !== 'undefined') ? showNav : true;
      %> <% const footerVisible = (typeof showFooter !== 'undefined') ?
      showFooter : true; %> <% if (navVisible) { %> <%-
      include('partials/header') %> <% } %>

      <main><%- body %></main>

      <% if (footerVisible) { %> <%- include('partials/footer') %> <% } %>
    </div>

    <!-- Page-specific Scripts -->
    <% if (pageJS) { %>
    <script type="module" src="<%= pageJS %>"></script>
    <% } %>

    <script type="module" src="/js/base.js"></script>
  </body>
</html>
